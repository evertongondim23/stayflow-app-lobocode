# Docker (frontend)

Este repositório contém **apenas** o compose do app:

| Arquivo | Uso |
|---------|-----|
| `docker-compose.vps-app.yml` | Build Vite + Nginx + labels Traefik |

Variáveis: **`.env` na raiz** (ver `.env.example`). O **Traefik** não está aqui — sobe no repositório da API (`der-api-lobocode`) ou equivalente.
